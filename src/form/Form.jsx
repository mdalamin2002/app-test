import React from 'react';

const Form = () => {
    return (
        <div>
            <form action="">
                <h1>Form</h1>
                <input type="text" />
                <button>Submit</button>

            </form>
        </div>
    );
};

export default Form;
