import React from 'react';

const Form = () => {
    return (
        <div>
            <form action="">
                <input type="text" />
                <button>Submit</button>
            </form>
        </div>
    );
};

export default Form;
