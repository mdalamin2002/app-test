import React from 'react';

const Form = () => {
    return (
        <div>
            <form action="">
                <input type="email" />
                <button>Submit</button>
            </form>
        </div>
    );
};

export default Form;
