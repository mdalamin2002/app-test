import React from 'react';

const Form = () => {
    return (
        <div>
            <form action="">
                <input type="text" />
                <button>Submit</button>
                <button>Submit1</button>
                <button>Submit2</button>
            </form>
        </div>
    );
};

export default Form;
